name: Install backlog-mcp
description: Install the backlog-mcp binary from github.com/corbym/backlog-mcp and expose its path

outputs:
  binary-path:
    description: Absolute path to the installed backlog-mcp binary
    value: ${{ steps.install.outputs.binary-path }}

runs:
  using: composite
  steps:
    - uses: actions/setup-go@v5
      with:
        go-version: stable
        cache: false

    - name: Install binary
      id: install
      shell: bash
      run: |
        go install github.com/corbym/backlog-mcp@latest
        echo "binary-path=$(go env GOPATH)/bin/backlog-mcp" >> "$GITHUB_OUTPUT"
